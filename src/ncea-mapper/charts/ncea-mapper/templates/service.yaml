apiVersion: v1
kind: Service
metadata:
  name: {{ include "ncea-mapper.fullname" . }}
  labels:
    {{- include "ncea-mapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ncea-mapper.selectorLabels" . | nindent 4 }}
